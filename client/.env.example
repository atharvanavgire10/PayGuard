# PayGuard client configuration. Copy to client/.env for a non-default API location.
# Vite inlines VITE_* variables into the production bundle, so never put a secret here.

# Base URL of the PayGuard API. Falls back to http://localhost:5000/api when unset.
VITE_API_URL=http://localhost:5000/api
